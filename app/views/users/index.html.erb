<p id="notice"><%= notice %></p>

<h1>Pick a username</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <p><%= link_to user.username, matches_path(user: user.id) %></p>
    <% end %>
  </tbody>
</table>

<br>
